{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --transpile-only --project tsconfig.server.json -r dotenv/config -r tsconfig-paths/register src/server.ts"
}
