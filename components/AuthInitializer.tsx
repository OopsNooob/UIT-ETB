"use client";

/**
 * AuthInitializer - Initializes auth state on app load
 * The useAuth hook automatically restores state from localStorage
 * when the first component using it mounts
 */
export default function AuthInitializer() {
  // This component is just a placeholder
  // All auth initialization happens in the useAuth hook's useEffect
  return null;
}
